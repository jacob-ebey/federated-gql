{
  "private": true,
  "name": "federated-gql",
  "version": "0.0.1",
  "description": "An example of the power of webpack federated modules while paired with Vercel to host a federated graphql api.",
  "main": "index.js",
  "author": "Jacob Ebey",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "wsrun build",
    "deploy": "wsrun deploy",
    "setup:aws": "wsrun setup:aws",
    "start": "node ./packages/gateway-repo/public/main.js"
  },
  "devDependencies": {
    "wsrun": "^5.2.0"
  }
}
